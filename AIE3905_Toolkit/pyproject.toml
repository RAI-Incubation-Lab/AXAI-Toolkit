[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aie3905-axai-toolkit"
version = "0.2.0"
description = "Explainability teaching examples and a student-project audit harness"
requires-python = ">=3.10,<3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
